using UnityEngine;

public class VideoFile : MonoBehaviour {
    public string path;
    //public MediaPlayerCtrl player;
    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }

    public void updateContent() {
        //player.m_strFileName = path;
        //player.Play();
    }
}
